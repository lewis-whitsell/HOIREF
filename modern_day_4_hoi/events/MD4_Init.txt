add_namespace = init

#PUT ANY EFFECTS YOU WANT TO HAPPEN ON INIT, in init.1 not init.0
#For whatever reason, anything outside of triggering an event doesn't actually work in init.0
#USE INIT.1
country_event = {
	id = init.0
	
	#major = yes
	hidden = yes
	#fire_only_once = yes
	is_triggered_only = yes
	
	immediate  = {
		country_event = {id = init.1} #Initial Pop up at the start of the game
		###DON't PUT ANY OTHER EFFECTS HERE###
	}	
}

#Creates the pop up at the start of the game that explains things
country_event = {
	id = init.1
	title = init.1.t
	
	desc = {
		text = init.1.d
		trigger = { not = { tag = SYR tag = FSA tag = NUS tag = ROJ tag = TUR tag = KUR tag = IRQ has_idea = GCC_member tag = PER } }
	}
	desc = { text = init.1.d_Syria trigger = { tag = SYR } }
	desc = { text = init.1.d_FSA trigger = { tag = FSA } }
	desc = { text = init.1.d_AL_NUSRA trigger = { tag = NUS } }
	desc = { text = init.1.d_ROJ trigger = { tag = ROJ } }
	desc = { text = init.1.d_TUR trigger = { tag = TUR } }
	desc = { text = init.1.d_KUR trigger = { tag = KUR } }
	desc = { text = init.1.d_IRQ trigger = { tag = IRQ } }
	desc = { text = init.1.d_GCC trigger = { has_idea = GCC_member } }
	desc = { text = init.1.d_PER trigger = { tag = PER } }
	desc = { text = init.1.d_USA trigger = { tag = USA } }
	desc = { text = init.1.d_SOV trigger = { tag = SOV } }
	
	is_triggered_only = yes
	
	
	immediate  = {
		hidden_effect = {
			#Executes PP & NU system just incase they don't click both option A & B
			#set_pp_gain = yes
			#set_NU_gain = yes	
			country_event = {id = brotherhood.0 } #Sets muslim_brotherhood relations
			country_event = {id = brotherhood.2 }
			set_country_flag = { flag = debt_counting value = 25 }
			set_country_flag = { flag = total_AB_wars value = 0 }
			country_event = { id = debt_count.1 days = 75 }
			country_event = { id = neighbor_drift.1 } #outlook drift values
			country_event = { id = neighbor_drift.2 }
			country_event = { id = low_stability.1} #Is executed twice, here and the decision intentionally ~Gearz
			country_event = { id = init_approval_rating.1 }
			country_event = { id = state_update.2 } #states debuff 
			#country_event = { id = GDPC_Update.0 days = 90 } #Auto-update GDP/C 
			country_event = { id = set_spirit.1 } #sets multi-ethnic spirit
			country_event = { id = set_spirit.2 } #sets internal faction flags
			country_event = { id = set_spirit.3 days = 3 } #sets economy flags for approval rating
			
			
			#Set NATO tech sharing
			if = {
				limit = { has_dlc = "Together for Victory" }
				every_country = {
					limit = { has_idea = NATO_member }
					add_to_tech_sharing_group = NATO_Tech_Share
				}
			}
		}
	}
	
	option = {
		name = init.1.e
	}
	
	
}

#This is only fired for newly formed countries created mid game e.g released nations. There is no reason to trigger this for every country
country_event = {
	id = init.2
	
	hidden = yes
	#fire_only_once = yes
	
	is_triggered_only = yes
	
	immediate  = {
		#set_pp_gain = yes
		#set_NU_gain = yes
		
		set_country_flag = { flag = debt_counting value = 25 }
		country_event = { id = debt_count.1 days = 75 }
		country_event = { id = neighbor_drift.1 } #outlook drift values
		country_event = { id = neighbor_drift.2 }country_event = { id = state_update.2 } #states debuff 
		country_event = { id = set_spirit.1 } #sets multi-ethnic spirit
		country_event = { id = set_spirit.2 } #sets internal faction flags
		country_event = { id = set_spirit.3 days = 3 } #sets economy flags for approval rating
	}
	
	option = {
		name = init.2.a
	}
}
#version 0.1 warning only 2 years of gameplay
news_event = {
	id = init.3
	title = init.3.t
	desc = init.3.d
	picture = GFX_news_md4
	
	#hidden = yes
	#fire_only_once = yes
	
	is_triggered_only = yes
	
	
	option = {
		name = init.3.a
		#this = {gearz_test = yes}
	}
	
	
}